- name: base
  hosts: validators
  gather_facts: yes
  become_method: sudo
 
  tasks:
    - name: install git
      ansible.builtin.apt:
        name: git
        state: installed
      become: yes
    - name: install gcc
      ansible.builtin.apt:
        name: gcc
        state: installed
      become: yes
