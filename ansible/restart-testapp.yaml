- name: restart celestiad
  hosts: validators
  become_method: sudo
  gather_facts: yes
 
  tasks:
    - name: restart app
      ansible.builtin.systemd:
        name: celestiad
        state: restarted
      become: yes
