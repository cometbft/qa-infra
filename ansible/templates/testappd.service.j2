# /etc/systemd/system/testappd.service
[Unit]
Description=Testapp Node
After=network.target

[Service]
Type=simple
User={{ ansible_user_id }}
WorkingDirectory={{ ansible_user_dir }}
ExecStart={{ansible_user_dir }}/go/bin/node {{ tm_home }}config/app.toml
Environment=TMHOME={{ tm_home }}
Restart=on-failure
RestartSec=3
LimitNOFILE=4096

[Install]
WantedBy=multi-user.target
